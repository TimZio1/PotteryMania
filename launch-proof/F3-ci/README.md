# F3 — CI system (proof pack)

**No green CI → no launch. Period.**

## Required artifacts

- [ ] **Screenshot or public URL** of a successful CI run (GitHub Actions, Railway, etc.).
- [ ] Same run must show explicitly:
  - **TypeScript** passed (`tsc` or equivalent step green).
  - **Tests** passed (Vitest/Jest step green).

**Suggested filenames:** `f3-ci-run-green.png` or paste `f3-ci-run-url.txt` with one line: the URL to the passing run.

---

Optional: attach raw log export `f3-ci-log.txt` (redact secrets).
